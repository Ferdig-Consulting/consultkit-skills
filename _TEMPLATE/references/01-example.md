# Stage 1 — Example

Replace this file with the first stage of your skill. Keep it focused on a single decision/output.

The dual-load contract from the parent SKILL.md:

- Agents on `pcc-apps` Read this from disk.
- Off-repo agents WebFetch the raw URL.

If a downstream stage needs context from this one, link it explicitly so the next agent knows what to carry forward.
